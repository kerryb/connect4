<.table
  id="c4-players"
  rows={@players}
  row_class={
    fn player ->
      if @current_player && @current_player.id == player.id,
        do: "bg-purple-100 hover:!bg-purple-200 text-bold"
    end
  }
>
  <:col :let={player} label="Player"><%= player.name %></:col>
</.table>
